server:
  port: 3344

spring:
  application:
    name: springcloud-config-server
  #连接远程配置
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/chinahnygl/springcloud-config.git